service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Regla de desarrollo: permite leer y escribir mientras estés probando.
      // NO dejes esto en producción sin seguridad adicional.
      allow read, write: if true;
    }
  }
}
